WORKFLOW-REVIEW:

I cannot approve the workflow as complete/correct yet. The overall large -> small -> coupled concept is sound, but the current coupled production run is not deliverable because the arch-loading proof is incomplete and there is a likely stale-drive implementation defect.

1. The 3-scale method is defensible in principle: small-model displacements are IDW-mapped, rigid body motion is removed in [couple_idw.py](C:/Users/Wade/Desktop/Tunnel_TX/05_One_Way_Simulation/couple_idw.py:55), and the coupled model is driven by cumulative residual boundary targets in [couple_full4.f3dat](C:/Users/Wade/Desktop/Tunnel_TX/05_One_Way_Simulation/couple_full4.f3dat:148). But `cb_control0` was created with 28,304 driven gridpoints, while `couple_full4` reads 23,530. Since `couple_full4` restores `cb_control0` and does not clear old `drive/driven` groups or old `gp.extra`, the removed 4,774 near-tunnel y-end points may still be driven from stale targets. This must be fixed before delivery.

2. The blue-line arch is not proven loaded by the existing plots. The `~7 MN` mid reaction is mostly absolute preload: in-situ is `abs_normal=7.31977 MN`, stage 1 is `7.31856 MN`, and stage 4 is `7.17125 MN`. So the reaction plot is not a drive-increment proof. The convergence metric is also diluted: [couple_qa_funcs.fis](C:/Users/Wade/Desktop/Tunnel_TX/05_One_Way_Simulation/couple_qa_funcs.fis:86) averages all mid-band balls with `rr > 0.5`, giving 270,871 balls, not inner-wall blue-line balls. Correct metrics are inner-wall `r~1.35-2 m` D-line radial convergence, arch/sidewall reaction increment versus control0, and sector reaction/stress increments versus in-situ.

3. The blue-line load region should be sidewall + crown + sidewall, excluding fixed corners and the bottom gap. In angle terms: crown-only is `0-180 deg`; full blue-line should include lower sidewalls but exclude the fixed-end bands. Since the artifact cracks are `259.8-264.8 deg`, I would exclude at least `250-300 deg` on the left/bottom side and a symmetric right-corner band around `300-320 deg`, then refine from actual ball geometry. Do not report `ang~262 deg` as invert cracking.

4. Visual verification protocol: for every stage, generate a fixed-scale figure pack with a D-shape cross-section showing blue-line mask, red fixed-end masks, and cracks; an unwrapped blue-line convergence profile; an unwrapped coupling reaction/stress profile; and a separate artifact panel for y-end/fixed-corner damage. Counts in plots must match log/CSV counts exactly. Both reviewers should sign off on these stage figures before delivery.

5. Additional checks: clear all old `drive` groups/extras before loading targets; make count mismatch a hard abort; recompute target-error after each stage; report absolute and increment reactions separately; replace `couple_evolution.png` because its title “invert tension” is now wrong; and document the active `couple_bnd.txt` filtering against `couple_bnd_ORIG.txt`.

Joint conclusion: the physics workflow is salvageable, but the current result package is not yet a correct final deliverable. The blue-line arch currently has 0 demonstrated cracks, and whether it was meaningfully loaded remains unresolved until the corrected diagnostics are run.
